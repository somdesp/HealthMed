apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui-app-latest
  labels:
    app: ui-app-latest
spec:
  replicas: 2
  selector:
    matchLabels:
      service: ui-app-latest
  template:
    metadata:
      labels:
        app: ui-app-latest
        service: ui-app-latest
    spec:
      containers:
        - name: ui-app-latest
          image: somdesp/healthmed:ui_app_latest
          ports:
            - containerPort: 80
              protocol: TCP
          env:
            - name: API_LINK
              value: http://localhost:30083
---
apiVersion: v1
kind: Service
metadata:
  name: ui-app-latest-service
  labels:
    app: ui-app-latest
    service: ui-app-latest
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 80
      protocol: TCP
  selector:
    service: ui-app-latest
